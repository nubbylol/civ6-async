.idea
.git

# .NET build outputs
src/**/bin/
src/**/obj/

# Published release binaries are tracked (so they're downloadable from the
# repo). Intermediate publish artefacts and SDK debug symbols are not.
dist/cli/**/*.pdb
dist/cli/**/*.dbg
